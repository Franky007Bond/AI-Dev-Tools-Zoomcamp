# Waitly frontend

Host-stand UI for Waitly. Backend calls live in `src/api/client.js` and are mocked.

```bash
npm install
npm run dev      # http://localhost:5173/  (guest join: /join)
npm test
```
